# RoomToSave — app

This is the Next.js app. See the [repository README](../README.md) for the
project overview, the verification story, and deploy instructions.

```bash
npm install
npm run dev          # http://localhost:3000
npm run type-check
npm test
npm run build        # static export → ./out
node check-testcases.mjs
```
